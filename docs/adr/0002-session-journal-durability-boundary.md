# Session Journal 只保证进程崩溃恢复

第一版 Session Journal 对所有事件统一同步 append 并 flush，不区分同步事件和异步事件；在 Turn 结束和 Runtime 正常退出时执行 fsync，以保证 LANTU 进程崩溃后可恢复。写入失败时关闭并重新打开文件重试一次；第二次仍失败则停止当前 Turn，不把未持久化的结果继续交给模型。它不为操作系统崩溃或突然断电提供最后一条事件绝不丢失的保证；这是对工具调用延迟、实现复杂度与恢复可靠性的折中。
