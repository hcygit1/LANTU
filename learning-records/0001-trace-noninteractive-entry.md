# 能追踪非交互模式入口

学习者已能判断 `uv run lantu -p "..."` 是一次性非交互执行，并能从参数分支追踪到 `_run_prompt()` 和后续异步流程。这意味着下一课可以进入运行时对象的组装过程，而无需重复讲解命令入口。

## Evidence

学习者指出该命令只执行一次，进入 `run_prompt` 并异步执行后返回；需要继续区分“一次用户提示”和 Agent 内部可能发生的多轮工具调用。
